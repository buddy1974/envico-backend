// Placeholder for future LLM integration (Claude API, OpenAI, etc.)
// Replace callLLM body with real API call when ready.

export async function callLLM(_prompt: string): Promise<string> {
  throw new Error('LLM not configured — set API key and implement callLLM()');
}
